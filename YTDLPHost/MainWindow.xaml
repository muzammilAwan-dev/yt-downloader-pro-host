<Window x:Class="YTDLPHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:YTDLPHost.ViewModels"
        mc:Ignorable="d"
        Title="YT Downloader Pro" 
        Icon="Assets/icon.ico"
        Height="600" Width="900"
        MinHeight="450" MinWidth="700"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <DataTemplate x:Key="DownloadItemTemplate" DataType="{x:Type vm:DownloadItemViewModel}">
            <Border Background="{StaticResource SurfaceBrush}" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1" 
                    CornerRadius="10"
                    Padding="16,14"
                    Margin="0,0,0,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" 
                                Width="10" Height="10" 
                                CornerRadius="5"
                                Margin="0,0,12,0"
                                VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{Binding Task.Status, Converter={StaticResource StatusToColorConverter}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Task.Status}" Value="Downloading">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard Name="BlinkingAnimation">
                                                    <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.4" Duration="0:0:0.8"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <StopStoryboard BeginStoryboardName="BlinkingAnimation"/>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <TextBlock Grid.Column="1" 
                                   Text="{Binding DisplayTitle}" 
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center"/>

                        <Border Grid.Column="2" 
                                Background="{StaticResource SurfaceHoverBrush}"
                                CornerRadius="4"
                                Padding="6,2"
                                Margin="8,0,8,0"
                                Visibility="{Binding ResolutionDisplay, Converter={StaticResource ProgressToVisibilityConverter}}"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding ResolutionDisplay}"
                                       FontSize="11"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Border>

                        <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                            
                            <Button Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding ToggleLogCommand}"
                                    ToolTip="Logs"
                                    Margin="0,0,4,0">
                                <Path Data="M19,19H5V5H19M19,3H5A2,2 0 0,0 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5A2,2 0 0,0 19,3M17,17H7V15H17M17,13H7V11H17M13,9H7V7H13" 
                                      Fill="{StaticResource TextMutedBrush}" Width="16" Height="16" Stretch="Uniform"/>
                            </Button>

                            <Button Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Open Folder"
                                    Visibility="{Binding Task.Status, Converter={StaticResource StatusToActionsVisibilityConverter}}"
                                    Margin="0,0,4,0">
                                <Path Data="M20,6H12L10,4H4A2,2 0 0,0 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8A2,2 0 0,0 20,6Z" 
                                      Fill="{StaticResource TextSecondaryBrush}" Width="16" Height="16" Stretch="Uniform"/>
                            </Button>

                            <Button Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding DataContext.PlayFileCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Play Video"
                                    Visibility="{Binding Task.Status, Converter={StaticResource StatusToActionsVisibilityConverter}}"
                                    Margin="0,0,4,0">
                                <Path Data="M8,5.14V19.14L19,12.14L8,5.14Z" 
                                      Fill="{StaticResource TextSecondaryBrush}" Width="16" Height="16" Stretch="Uniform"/>
                            </Button>

                            <Button Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding DataContext.ResumeDownloadCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Resume"
                                    Visibility="{Binding IsResumable, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Margin="0,0,4,0">
                                <Path Data="M8,5.14V19.14L19,12.14L8,5.14Z" 
                                      Fill="{StaticResource SuccessBrush}" Width="16" Height="16" Stretch="Uniform"/>
                            </Button>

                            <Button Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding DataContext.PauseDownloadCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Pause"
                                    Visibility="{Binding CanBePaused, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Margin="0,0,4,0">
                                <Path Data="M14,19H18V5H14M6,19H10V5H6V19Z" 
                                      Fill="{StaticResource WarningBrush}" Width="16" Height="16" Stretch="Uniform"/>
                            </Button>

                            <Button Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding DataContext.CancelDownloadCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Cancel (Deletes partial files)"
                                    Visibility="{Binding CanBeCancelled, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                      Fill="{StaticResource ErrorBrush}" Width="16" Height="16" Stretch="Uniform"/>
                            </Button>
                            
                            <Button Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding DataContext.RemoveDownloadCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Remove from List"
                                    Visibility="{Binding CanBeRemoved, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                      Fill="{StaticResource TextMutedBrush}" Width="16" Height="16" Stretch="Uniform"/>
                            </Button>

                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1" Margin="22,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="{Binding ProgressDisplay}"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"/>

                        <TextBlock Grid.Column="1"
                                   Text="{Binding ResolutionDisplay}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   Margin="8,0,0,0"
                                   Visibility="{Binding ResolutionDisplay, Converter={StaticResource ProgressToVisibilityConverter}}"/>
                    </Grid>

                    <ProgressBar Grid.Row="2"
                                 Style="{StaticResource DownloadProgressBar}"
                                 Value="{Binding Progress, Mode=OneWay}"
                                 IsIndeterminate="{Binding Task.IsIndeterminate, Mode=OneWay}"
                                 Maximum="100"
                                 Margin="22,8,0,0"
                                 Visibility="{Binding Task.Status, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"/>

                    <Border Grid.Row="3"
                            Background="{StaticResource BackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Margin="22,12,0,0"
                            Padding="8"
                            Visibility="{Binding IsLogVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ScrollViewer Height="150" VerticalScrollBarVisibility="Auto">
                            <TextBox Text="{Binding Task.UiLogText, Mode=OneWay}"
                                     Background="Transparent"
                                     Foreground="{StaticResource TextSecondaryBrush}"
                                     BorderThickness="0"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     FontSize="11"
                                     FontFamily="Consolas, Monospace"/>
                        </ScrollViewer>
                    </Border>
                    
                    <Border Grid.Row="3"
                            Background="#15F44336"
                            CornerRadius="6"
                            Padding="8,6"
                            Margin="22,8,0,0"
                            Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="{Binding ErrorMessage}"
                                   FontSize="11"
                                   Foreground="{StaticResource ErrorBrush}"
                                   TextWrapping="Wrap"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>

        <Style x:Key="DownloadListItemContainer" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter HorizontalAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{StaticResource BackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="32" Height="32" 
                            CornerRadius="6"
                            Background="#FF0000"
                            Margin="0,0,12,0">
                        <Path Data="M19,9H15V3H9V9H5L12,16L19,9M5,18V20H19V18H5Z" 
                              Fill="White" Width="20" Height="20" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="YT Downloader Pro" 
                                   FontSize="17" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Native yt-dlp Host" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextMutedBrush}"/>
                            <TextBlock Text=" • Crafted with ⚡ by muzammilAwan-dev" 
                                       FontSize="11" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBrush}" 
                                       Margin="4,0,0,0"/>
                        </StackPanel>

                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1"
                        Style="{StaticResource ActionButtonStyle}"
                        Content="Clear Completed"
                        Command="{Binding ClearCompletedCommand}"
                        Visibility="{Binding HasCompletedDownloads, Converter={StaticResource BoolToVisibilityConverter}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconButtonStyle}"
                            Command="{Binding MinimizeToTrayCommand}"
                            ToolTip="Minimize to Tray"
                            x:Name="MinimizeTrayButton">
                        <Path Data="M19,13H5V11H19V13Z" 
                              Fill="{StaticResource TextSecondaryBrush}" Width="16" Height="16" Stretch="Uniform"/>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}"
                            Click="OnSettingsClick"
                            ToolTip="Settings">
                        <Path Data="M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.66 15.5,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.5,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.21,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.34 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.21,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.03 4.95,18.95L7.44,17.94C7.96,18.34 8.5,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.5,18.67 16.04,18.34 16.56,17.94L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z" 
                              Fill="{StaticResource TextSecondaryBrush}" Width="16" Height="16" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid Visibility="{Binding HasDownloads, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">
                    <Path Data="M19,9H15V3H9V9H5L12,16L19,9M5,18V20H19V18H5Z" 
                          Fill="{StaticResource SurfaceHoverBrush}" Width="64" Height="64" Stretch="Uniform"
                          HorizontalAlignment="Center"/>
                    <TextBlock Text="No active downloads" 
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,20,0,8"/>
                    <TextBlock Text="Click the Download button in YouTube to add videos" 
                               FontSize="13"
                               Foreground="{StaticResource TextMutedBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <ScrollViewer Visibility="{Binding HasDownloads, Converter={StaticResource BoolToVisibilityConverter}}"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="20,12"
                          PanningMode="VerticalOnly">
                <ListBox ItemsSource="{Binding Downloads}"
                         ItemTemplate="{StaticResource DownloadItemTemplate}"
                         ItemContainerStyle="{StaticResource DownloadListItemContainer}"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ScrollViewer.CanContentScroll="True"/>
            </ScrollViewer>
        </Grid>

        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Fill="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center">
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Ellipse.Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.3" Duration="0:0:1.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    <TextBlock Text="{Binding StatusText}" 
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Text="{Binding ActiveDownloadCount, Converter={StaticResource CountToTextConverter}}"
                           FontSize="12"
                           Foreground="{StaticResource TextMutedBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
